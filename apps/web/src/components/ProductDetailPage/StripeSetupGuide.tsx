'use client';

import React from 'react';
import { AlertCircle, ExternalLink } from 'lucide-react';
import { Card } from '@/components/ui/card';

export const StripeSetupGuide: React.FC = () => {
  // Stripe removed — no UI needed.
  return null;
};
